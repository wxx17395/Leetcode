#include <bits/stdc++.h>
using namespace std;

int main()
{
   int n;
   cin >> n;
   cin.sync();
   string a, b;
   getline(cin, a);
   getline(cin, b);
   cout << "a:" << a << ";b:" << b << ";" << endl; 
}